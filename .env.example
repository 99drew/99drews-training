# Opcional — só necessário se você implantar o servidor de push em /server
# pra ter notificação confiável do cronômetro com a tela bloqueada.
# Sem essas variáveis o app funciona normalmente, só com o fallback de melhor esforço.
# Ver README.md, seção "Notificação com tela bloqueada (opcional)".

VITE_PUSH_SERVER_URL=
VITE_VAPID_PUBLIC_KEY=
